import React, { FC } from "react"

type Props = {
    colorScheme: string
}

const Logo: FC<Props> = ({ colorScheme }) => {
    return (
        <span>
            {/*TODO: вставть логотип*/}
            Logo
        </span>
    )
}

export default Logo
